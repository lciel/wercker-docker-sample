box: wercker/ruby
build:
    steps:
        - script:
            name: test
            code: |
              hostname
              ls -R /cache
              touch /cache/file
              ls -R /cache
