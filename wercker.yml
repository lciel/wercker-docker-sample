box: wercker-labs/docker
build:
    steps:
        - script:
            name: test
            code: |
              hostname
              ls -R /cache
              touch /cache/file
              ls -R /cache
