box: wercker/ruby
build:
    steps:
        - script:
            name: test
            code: |
              hostname
              ls /cache
              touch /cache/file
              ls /cache
